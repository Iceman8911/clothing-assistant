export const enum gCustomRouteEnum {
  HOME = "/",
  STOCK = "/inventory",
  REPORTS = "/reports",
  SETTINGS = "/settings",
}

export const enum gStatusEnum {
  // NEUTRAL,
  SUCCESS,
  INFO,
  WARNING,
  ERROR,
}
