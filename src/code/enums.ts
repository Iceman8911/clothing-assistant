export const enum CustomRoute {
  HOME = "/",
  STOCK = "/inventory",
  REPORTS = "/reports",
  SETTINGS = "/settings",
}

export const enum gStatus {
  // NEUTRAL,
  SUCCESS,
  INFO,
  WARNING,
  ERROR,
}
